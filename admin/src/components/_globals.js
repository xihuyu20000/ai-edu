import Vue from "vue";
// 这里的注册名，必须是小写，并且在调用的时候，也要小写
Vue.component("create-form", "@/components/CreateForm");
Vue.component("edit-form", "@/components/EditForm");
Vue.component("common-table", "@/components/CommonTable");
